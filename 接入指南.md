# SsCfg 接入指南

这份文档不是给仓库维护者看的。

这份文档是给“想把 `SsCfg` 接到自己项目里的人”看的。

目标很简单：

- 让你的程序能读取这个仓库的包列表
- 让你的程序能显示包信息
- 让你的程序能下载某个包的原始 JSON
- 让你的程序能把下载到的包内容真正用起来

如果你只想知道最少要接什么，先看这一段。

## 1. 最小接入流程

你的程序只要做这几步，就能接：

1. 下载仓库根目录的 `index.json`
2. 读取里面的 `packs` 列表
3. 把 `name / author / summary / version / date / southsideVersion` 展示给用户
4. 用户选中某个包后，读取它的 `downloadUrl`
5. 再去下载这个 `downloadUrl` 指向的原始 JSON
6. 把这个 JSON 按你自己的业务格式解析
7. 把解析后的内容接进你自己的 mod 或工具

就这么多。

## 2. 仓库地址

仓库主页：

```text
https://github.com/bedkillerspacex-boop/SsCfg
```

当前默认分支：

```text
master
```

索引地址：

```text
https://raw.githubusercontent.com/bedkillerspacex-boop/SsCfg/refs/heads/master/index.json
```

如果你的项目允许用户自己填仓库地址，建议至少兼容这几种输入：

- GitHub 仓库页面地址
- `owner/repo`
- 直接的 raw `index.json` 地址

## 3. 你需要读哪些文件

对接入方来说，最重要的只有两类文件：

```text
SsCfg/
├─ index.json
└─ packs/
   ├─ A.json
   ├─ B.json
   └─ C.json
```

含义：

- `index.json`
  - 对外发布的包目录
  - 用来做搜索、列表、分页、详情展示
- `packs/*.json`
  - 某一个具体包的原始正文
  - 真正要拿来用的内容在这里

接入方通常**不需要**直接读取 `publisher_meta/`。

## 4. `index.json` 结构

当前 `index.json` 大概像这样：

```json
{
  "schemaVersion": 1,
  "generatedAt": "2026-05-30T08:51:26Z",
  "maxPackId": 1,
  "packs": [
    {
      "id": 1,
      "name": "demo",
      "author": "bedkillerspacex-boop",
      "summary": "first demo pack",
      "version": 1,
      "date": "2026-05-30",
      "southsideVersion": "1.0.0",
      "sha256": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
      "downloadUrl": "https://raw.githubusercontent.com/bedkillerspacex-boop/SsCfg/refs/heads/master/packs/demo.json",
      "fileName": "demo.json",
      "sizeBytes": 61
    }
  ]
}
```

## 5. 你至少要用到哪些字段

### 列表展示最常用

- `id`
  - 唯一标识
- `name`
  - 包名
- `author`
  - 作者
- `summary`
  - 简介
- `version`
  - 发布版本号
- `date`
  - 日期
- `southsideVersion`
  - 适配的 Southside 版本

### 下载时最重要

- `downloadUrl`
  - 真正下载包原文的地址
- `sha256`
  - 你如果要做缓存校验，可以用它
- `sizeBytes`
  - 你如果要做展示或下载校验，可以用它

### 其他字段

- `fileName`
  - 便于展示真实文件名
- `generatedAt`
  - 当前索引是什么时候生成的
- `maxPackId`
  - 更偏维护用途，接入方一般不用

## 6. `packs/*.json` 应该怎么理解

这套仓库里，包文件被当成“原始内容文件”。

也就是说：

- 它不一定内置作者
- 不一定内置版本号
- 不一定内置日期
- 不一定和 `index.json` 拥有同样的元数据字段

接入方的正确思路是：

1. **用 `index.json` 拿元数据**
2. **用 `downloadUrl` 拿原始包正文**

不要反过来先假定包文件里一定有全部展示字段。

## 7. 程序里怎么用包正文

这里取决于你自己的 mod 设计。

最常见的模式有两种。

### 模式 A：把下载到的 JSON 原样交给你的业务层

适合你自己的 mod 已经定义好了包结构。

流程：

1. 从 `index.json` 选包
2. 下载 `downloadUrl`
3. 把 JSON 解出来
4. 按你的业务结构消费

### 模式 B：只把它当文本配置源

适合你只想把这个仓库当成某种“云配置包”。

流程：

1. 下载原始 JSON
2. 抽出你关心的字段
3. 写入本地配置、缓存或内存状态

## 8. 搜索建议

最简单的搜索逻辑：

对 `index.json` 里的每个包，匹配这些字段：

- `id`
- `name`
- `author`
- `summary`
- `southsideVersion`
- `fileName`

只要其中任意一个字段包含搜索词，就显示这个包。

## 9. 缓存建议

推荐分两层缓存。

### 索引缓存

缓存 `index.json`

用途：

- 打开界面更快
- 网络失败时还能显示上一次的列表

### 包正文缓存

缓存 `downloadUrl` 下载下来的原始 JSON

用途：

- 用户重复选择同一个包时，不用每次都重新下载

## 10. 缓存失效一定要做

最典型的问题是：

1. 你拿到了新的 `index.json`
2. 用户界面已经显示新版本信息
3. 但本地还在读旧的包正文缓存
4. 最后程序里实际用的还是旧内容

所以推荐做法：

- 同步新索引后，至少检查已缓存包的 `sha256`
- 如果 `sha256` 变了，就重新下载对应包
- 或者更简单一点，索引更新后直接让当前选中包缓存失效

## 11. 推荐错误提示

最好至少区分这些错误：

- `index.json 下载失败`
- `index.json 不是合法 JSON`
- `downloadUrl 下载失败`
- `包正文不是合法 JSON`
- `包正文结构不符合你的业务要求`
- `缓存校验失败`

不要只弹一个“失败了”。

错误越具体，越容易排查。

## 12. 推荐兼容策略

如果你想让用户填地址更方便，建议兼容这几种输入：

### 仓库页地址

```text
https://github.com/bedkillerspacex-boop/SsCfg
```

### 短格式

```text
bedkillerspacex-boop/SsCfg
```

### 直接 raw 地址

```text
https://raw.githubusercontent.com/bedkillerspacex-boop/SsCfg/refs/heads/master/index.json
```

程序内部统一转成可请求的 raw `index.json` 地址就行。

## 13. 最小伪代码

```text
index = GET(index.json)
packs = index["packs"]

show_list(packs)

selected = user_choose_pack()
pack_json = GET(selected["downloadUrl"])

use_pack(pack_json)
```

如果你还做缓存校验，可以再加一层：

```text
if cache.sha256 != selected["sha256"]:
    pack_json = GET(selected["downloadUrl"])
    save_cache(pack_json, selected["sha256"])
else:
    pack_json = load_cache()
```

## 14. 一句话总结

接这个仓库，本质上就两步：

1. 先读 `index.json` 做列表和元数据展示
2. 再按 `downloadUrl` 读具体包正文

只要你把“索引”和“正文”分开处理，再把缓存失效做好，`SsCfg` 就很容易接进任何 mod、启动器、脚本工具或客户端里。
