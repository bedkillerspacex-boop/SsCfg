# SsCfg 维护指南

这份文档是给仓库维护者看的。

目标只有一个：

把仓库里的 `packs/`、`publisher_meta/` 和根目录的 `index.json` 维护正确，让外部 mod 或工具可以正常读取、显示、下载和使用这些包。

如果你不想研究发布器内部实现，只想把仓库维护对，照着这份文档一步一步做就够了。

## 1. 当前仓库结构

当前这套仓库不是老式的“直接改 pack JSON 元数据”的结构，而是三层：

```text
SsCfg/
├─ packs/
│  ├─ A.json
│  ├─ B.json
│  └─ C.json
├─ publisher_meta/
│  ├─ registry.json
│  └─ packs/
│     ├─ 1.json
│     ├─ 2.json
│     └─ 3.json
├─ index.json
└─ publisher/
   └─ sscfg_publisher.py
```

含义：

- `packs/`
  - 这里放每个包自己的原始 JSON 文件
  - 这些文件被当成“只读源文件”
  - `publisher` 不应该往里面写作者、版本、日期之类的发布元数据
- `publisher_meta/registry.json`
  - 维护 `maxPackId`
  - 维护 `sourceFile -> id` 的绑定关系
- `publisher_meta/packs/<id>.json`
  - 每个已发布包对应一个 sidecar 元数据文件
  - 这里维护 `name`、`author`、`summary`、`version`、`date`、`southsideVersion`
- `index.json`
  - 对外发布用的索引文件
  - 外部接入方先读它，再按 `downloadUrl` 去下具体包
- `publisher/sscfg_publisher.py`
  - 维护和发布用工具

最重要的一句话：

**不要手改 `packs/*.json` 里的发布元数据。发布元数据统一归 `publisher_meta/` 管。**

## 2. 最重要的规则

### 2.1 `packs/*.json` 是只读源文件

你可以新增、删除、替换、重命名包文件本身，但不要把下面这些字段当成 pack 文件的维护目标：

- 作者
- 简介
- 版本号
- 日期
- Southside 版本

这些字段应该写进 `publisher_meta/packs/<id>.json`，不是写进 `packs/*.json`。

### 2.2 包 ID 由 publisher 管

- 每个包都有唯一整数 `id`
- `id` 一旦分配就不要改
- `id` 不要求写进包文件本身
- `registry.json` 和 `publisher_meta/packs/<id>.json` 才是 ID 的真实来源

### 2.3 分支必须统一

当前仓库远程使用的是：

```text
bedkillerspacex-boop/SsCfg
```

当前发布索引和 `downloadUrl` 应该统一指向当前实际分支。

如果仓库主分支是 `master`，那：

- `index.json` 从 `master` 发布
- `downloadUrl` 也必须写成 `master`

不要一部分用 `master`，另一部分用 `main`。

## 3. 包文件应该长什么样

这套架构里，`packs/*.json` 被视为“包内容原件”。

也就是说，publisher 只要求它：

1. 是合法 JSON
2. 位于 `packs/` 目录下
3. 文件扩展名是 `.json`

最小示例：

```json
{
  "entries": [
    "demo line 1",
    "demo line 2"
  ]
}
```

注意：

- publisher 不依赖 pack 内部必须有 `id`
- publisher 也不要求 pack 里必须有 `author`、`version`、`date`
- pack 里到底是 `entries` 还是别的业务字段，取决于你未来的 mod 需要什么
- 只要你决定了包格式，后续接入方按那个格式读就行

## 4. sidecar 元数据应该长什么样

`publisher_meta/packs/<id>.json` 大概像这样：

```json
{
  "schemaVersion": 1,
  "id": 1,
  "name": "demo",
  "author": "bedkillerspacex-boop",
  "summary": "first demo pack",
  "version": 1,
  "date": "2026-05-30",
  "southsideVersion": "1.0.0",
  "sourceFile": "packs/demo.json",
  "createdAt": "2026-05-30T00:00:00Z"
}
```

字段说明：

- `id`
  - 包的唯一发布 ID
- `name`
  - 给外部用户看的包名
- `author`
  - 作者名
- `summary`
  - 简短介绍
- `version`
  - 这个包自己的发布版本号
  - 当前是手动维护，不自动递增
- `date`
  - 这个包当前版本的日期
- `southsideVersion`
  - 这个包适配的 Southside 版本
- `sourceFile`
  - 绑定到哪个 `packs/*.json`
- `createdAt`
  - 首次创建这条 sidecar 记录的时间

## 5. `registry.json` 的作用

`publisher_meta/registry.json` 大概像这样：

```json
{
  "schemaVersion": 1,
  "maxPackId": 3,
  "bindings": {
    "packs/demo.json": 1,
    "packs/demo2.json": 2,
    "packs/demo3.json": 3
  }
}
```

作用：

- 记录当前已经用到的最大 ID
- 记录哪个源文件绑定了哪个 ID

维护时记住：

- 新包发布时，`maxPackId` 要加一
- `bindings` 要新增对应关系
- 如果做重新绑定，要同时更新 `bindings` 和对应 sidecar 的 `sourceFile`

## 6. `index.json` 应该长什么样

当前 `index.json` 结构大概像这样：

```json
{
  "schemaVersion": 1,
  "generatedAt": "2026-05-30T08:51:26Z",
  "maxPackId": 1,
  "packs": [
    {
      "id": 1,
      "name": "demo",
      "author": "bedkillerspacex-boop",
      "summary": "first demo pack",
      "version": 1,
      "date": "2026-05-30",
      "southsideVersion": "1.0.0",
      "sha256": "xxxxxxxx",
      "downloadUrl": "https://raw.githubusercontent.com/bedkillerspacex-boop/SsCfg/refs/heads/master/packs/demo.json",
      "fileName": "demo.json",
      "sizeBytes": 61
    }
  ]
}
```

字段说明：

- `schemaVersion`
  - 索引结构版本
- `generatedAt`
  - 索引生成时间
- `maxPackId`
  - 当前仓库已经分配到的最大 ID
- `packs`
  - 对外可用包列表

每个包里当前至少要维护：

- `id`
- `name`
- `author`
- `summary`
- `version`
- `date`
- `southsideVersion`
- `sha256`
- `downloadUrl`
- `fileName`
- `sizeBytes`

## 7. 最常见的维护任务

### 7.1 新增一个包

按这个顺序做：

1. 把新的 `.json` 文件放进 `packs/`
2. 用 `publisher/sscfg_publisher.py` 扫描仓库
3. 选中这个未发布文件
4. 点“发布当前项”
5. 填 `name / author / summary / version / date / southsideVersion`
6. 点“保存元数据”
7. 点“重建索引”
8. 检查生成的 `index.json`
9. 提交并推送

### 7.2 修改一个已有包

按这个顺序做：

1. 直接修改 `packs/` 里的对应源文件
2. 如果发布信息也要变，再改对应 sidecar 元数据
3. 重建 `index.json`
4. 检查 `sha256`、`sizeBytes`、`downloadUrl`
5. 提交并推送

### 7.3 删除一个包

当前最稳的做法：

1. 删除 `packs/` 里的源文件
2. 删除 `publisher_meta/packs/<id>.json`
3. 从 `publisher_meta/registry.json` 删掉对应 `bindings`
4. 重建 `index.json`
5. 提交并推送

注意：

- `maxPackId` 不需要回退
- 已经分配过的 ID 不要复用

### 7.4 重命名包文件

不要只改文件名就结束。

按这个顺序做：

1. 修改 `packs/` 里的文件名
2. 打开 publisher
3. 选中原有记录
4. 用“重新绑定”把它绑定到新文件
5. 保存元数据
6. 重建 `index.json`
7. 提交并推送

## 8. `sha256`、`sizeBytes`、`downloadUrl` 怎么来的

这三个值都来自实际源文件：

- `sha256`
  - 对 `packs/*.json` 原始字节做 SHA-256
- `sizeBytes`
  - 源文件实际字节数
- `downloadUrl`
  - 指向远程仓库 raw 文件地址

也就是说：

**只要改了源文件内容，就必须重建 `index.json`。**

## 9. 提交前检查清单

每次推送前，至少过一遍这些：

- 新包文件是不是放在 `packs/` 里
- 包文件是不是合法 JSON
- `sourceFile` 有没有写错
- `registry.json` 的 `bindings` 对不对
- `publisher_meta/packs/<id>.json` 是否存在
- `index.json` 里有没有对应记录
- `sha256` 是否是最新的
- `sizeBytes` 是否匹配当前文件
- `downloadUrl` 是否能指向真实文件
- `version / date / southsideVersion` 是否正确

## 10. 推荐命令

如果你不用 GUI，也可以手动这样做：

预览：

```powershell
python .\publisher\sscfg_publisher.py --repo E:\DESKTOP\project\SouthsideConfigLoader --dry-run
```

重建索引：

```powershell
python .\publisher\sscfg_publisher.py --repo E:\DESKTOP\project\SouthsideConfigLoader --write-index
```

提交并推送：

```powershell
python .\publisher\sscfg_publisher.py --repo E:\DESKTOP\project\SouthsideConfigLoader --commit --push
```

## 11. 最容易出错的地方

### 改了 `packs/*.json` 却没重建 `index.json`

后果：

- 外部程序拿到旧 `sha256`
- 外部程序拿到旧文件大小
- 展示信息和实际文件不一致

### 只改了文件名，没有做重新绑定

后果：

- sidecar 还指向旧文件
- 索引重建后会提示源文件缺失

### 手动乱改 `id`

后果：

- `registry.json`
- sidecar 文件名
- `index.json`

这三处会互相打架。

### 把发布元数据写回 pack 文件

后果：

- 破坏“只读源文件”边界
- 以后难以区分内容和发布信息

## 12. 一句话总结

维护这个仓库，本质上就三件事：

1. `packs/` 放好真实源文件
2. `publisher_meta/` 管好发布元数据和 ID 绑定
3. `index.json` 始终重建成最新状态

只要这三层没乱，外部 mod 和工具就能稳定接入。
